import './Header.css';
import Logo from '../Logo';
import Navigation from '../Navigation/Navigation';

function Header() {
  return (
    <header>
      <Logo />
      <Navigation />
    </header>
  );
}

export default Header;
